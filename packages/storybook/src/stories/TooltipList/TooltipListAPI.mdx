import { Meta } from '@storybook/blocks';
import { TypeSourceViewer } from '@df-storybook/utils/TypeSourceViewer';

<Meta title="Локальные компоненты/TooltipList/API" />

# TooltipList API

## TooltipListProps

<TypeSourceViewer
  language="ts"
  filePath="packages/ui-kit/src/components/Tooltip/TooltipList.tsx"
  typeName="TooltipListProps"
/>
> Остальные пропсы наследуются от `TooltipProps` (кроме `text`, `children`)
