import { Meta, Stories } from '@storybook/blocks';
import * as CanvasEmbedIconButtonStories from './CanvasEmbedIconButton.stories';

<Meta of={CanvasEmbedIconButtonStories} name="Docs" />

# Canvas.EmbedIconButton

Незалитая кнопка-иконка внутри canvas-ячейки `TableCanvas`.

## Ключевые особенности

- Несколько вариантов `view` и `buttonSize`
- Поддержка `disabled`

Описание типов — в разделе [API](?path=/docs/локальные-компоненты-tablecanvas-canvaselements-canvasembediconbutton-api--docs).

<Stories />
