import { Meta } from '@storybook/blocks';
import { TypeSourceViewer } from '@df-storybook/utils/TypeSourceViewer';

<Meta title="Локальные компоненты/TableCanvas/HoverEffects/API" />

# HoverEffects API

## HoverEffectsConfig

`tableConfig.hoverEffects` — единый конфиг эффектов при наведении. Значения,
цвета по зонам строки и приоритет слоёв описаны в комментариях к типу.

<TypeSourceViewer
  language="ts"
  filePath="packages/ui-kit/src/components/TableGlide/types.ts"
  typeName="HoverEffectsConfig"
/>
