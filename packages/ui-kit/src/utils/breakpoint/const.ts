import { breakPoints } from '@ui-kit/constants';

import { Breakpoint, BreakpointConfig } from './types';

export const DEFAULT_BREAKPOINT: Breakpoint = 'xl';

export const DEFAULT_BREAKPOINT_CONFIG: BreakpointConfig = breakPoints;
