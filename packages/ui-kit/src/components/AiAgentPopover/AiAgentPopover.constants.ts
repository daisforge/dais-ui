export const COLORS = {
  purple: '#b559f3',
  blue: '#7496fd',
};

export const X_DEFAULT_OFFSET = 26;

export const DRAG_THRESHOLD = 5;

export const ELEMENT_INDENTATION = 56;

export const DEFAULT_POSITION = {
  x: window.innerWidth - 100,
  y: window.innerHeight - 100,
};

export const SAFETY_MARGIN = 10; // Отступ от краёв экрана (когда поведение меняется на auto)

export const LOCAL_STORAGE_DEFAULT_KEY = 'ai-agent-popover-position';
