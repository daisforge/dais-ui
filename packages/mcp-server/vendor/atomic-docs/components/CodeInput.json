{
  "name": "CodeInput",
  "slug": "components/CodeInput",
  "category": "components",
  "url": "https://plasma.sberdevices.ru/sdds-finai/components/CodeInput/",
  "content": "# CodeInput CodeInput Компонент  `CodeInput`  используется для ввода OTP-кодов в виде крупных символов с круговыми индикаторами. ## Быстрый старт Быстрый старт ```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <CodeInput\n            caption=\"Введите код из СМС\"\n            captionAlign=\"center\"\n        />\n    );\n}\n```\n\n## Размер Размер Размер задается с помощью свойства  `size` . Ширина компонента также может регулироваться при помощи свойства  `width` : ```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <div style={{ flexDirection: 'column', gap: '2rem' }}>\n            <CodeInput\n                size=\"l\"\n                caption=\"Осталось попыток: 4\"\n                captionAlign=\"center\"\n            />\n            <CodeInput\n                size=\"m\"\n                caption=\"Чтобы выслать вам код повторно подождите: 20 секунд\"\n                captionAlign=\"left\"\n            />\n            <CodeInput\n                size=\"s\"\n                caption=\"Не передавайте никому код из СМС. Иначе ваш аккаунт могут взломать\"\n                captionAlign=\"center\"\n            />\n        </div>\n    );\n}\n```\n\n```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <div>\n            <CodeInput\n                width=\"30\"\n                caption=\"Не передавайте никому код из СМС. Иначе ваш аккаунт могут взломать\"\n                captionAlign=\"center\"\n            />\n        </div>\n    );\n}\n```\n\n## Длина кода Длина кода Длина кода задается с помощью свойства  `codeLength` : ```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <div style={{ flexDirection: 'column', gap: '2rem' }}>\n            <CodeInput codeLength={4} />\n            <CodeInput codeLength={5} />\n            <CodeInput codeLength={6} />\n        </div>\n    );\n}\n```\n\n## Скрытый ввод Скрытый ввод Свойство  `hidden`  переводит компонент в режим скрытого ввода: вместо символов отображаются круговые индикаторы.\nЭто удобно для ввода PIN-кодов и других секретных данных: ```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <div style={{ flexDirection: 'column', gap: '2rem' }}>\n            <CodeInput caption=\"Обычный режим\" captionAlign=\"center\" />\n            <CodeInput hidden caption=\"Скрытый режим\" captionAlign=\"center\" />\n        </div>\n    );\n}\n```\n\n## Допустимые символы Допустимые символы За допустимые символы для ввода отвечает свойство  `allowedSymbols` . Задается строкой или регулярным выражением.\nБиблиотека экспортирует наиболее используемые вариации допустимых символов:\n `import { ONLY_DIGITS_PATTERN, ONLY_CHARS_PATTERN, ONLY_DIGITS_AND_CHARS_PATTERN } from '@salutejs/sdds-finai';` ## Поведение при вводе Поведение при вводе Поведение ячейки при вводе некорректного символа регулируется свойством  `itemErrorBehavior` .\nПри заполнении всех полей ввода срабатывает колбэк  `onFullCodeEnter` . При неверном вводе кода поведение компонента управляется свойством  `codeErrorBehavior` . Индикация ошибки — с помощью флага  `isError`  и колбэка  `setIsError` : ```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <div style={{ flexDirection: 'column', gap: '2rem' }}>\n            <CodeInput itemErrorBehavior=\"remove-symbol\" />\n            <CodeInput itemErrorBehavior=\"forbid-enter\" />\n        </div>\n    );\n}\n```\n\n```tsx\nimport React, { useState, Dispatch, SetStateAction } from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    const [disabledFirst, setDisabledFirst] = useState(false);\n    const [isErrorFirst, setIsErrorFirst] = useState(false);\n\n    const [disabledSecond, setDisabledSecond] = useState(false);\n    const [isErrorSecond, setIsErrorSecond] = useState(false);\n\n    const onFullCodeEnter = (\n        code: string,\n        errorSetter: Dispatch<SetStateAction<boolean>>,\n        disableSetter: Dispatch<SetStateAction<boolean>>\n    ) => {\n        disableSetter(true);\n\n        // имитация запроса на сервер\n        setTimeout(() => {\n            disableSetter(false);\n\n            if (code !== '123456') {\n                errorSetter(true);\n            }\n        }, 3000)\n    }\n\n    return (\n        <div style={{ flexDirection: 'column', gap: '2rem' }}>\n            <CodeInput\n                itemErrorBehavior=\"remove-symbol\"\n                codeErrorBehavior=\"remove-code\"\n                disabled={disabledFirst}\n                isError={isErrorFirst}\n                setIsError={setIsErrorFirst}\n                onFullCodeEnter={(code) => onFullCodeEnter(code, setIsErrorFirst, setDisabledFirst)}\n            />\n            <CodeInput\n                itemErrorBehavior=\"remove-symbol\"\n                codeErrorBehavior=\"keep\"\n                disabled={disabledSecond}\n                isError={isErrorSecond}\n                setIsError={setIsErrorSecond}\n                onFullCodeEnter={(code) => onFullCodeEnter(code, setIsErrorSecond, setDisabledSecond)}\n            />\n        </div>\n    );\n}\n```\n\n## Тип клавиатуры на мобильных устройствах Тип клавиатуры на мобильных устройствах Свойство  `inputMode`  управляет типом виртуальной клавиатуры, отображаемой на мобильных устройствах.\nПо умолчанию используется  `numeric`  — открывается числовая клавиатура. Значение  `text`  открывает стандартную текстовую клавиатуру: ```tsx\nimport React from 'react';\nimport { CodeInput } from '@salutejs/sdds-finai';\n\nexport function App() {\n    return (\n        <div style={{ flexDirection: 'column', gap: '2rem' }}>\n            <CodeInput\n                inputMode=\"numeric\"\n                caption=\"Числовая клавиатура (по умолчанию)\"\n                captionAlign=\"center\"\n            />\n            <CodeInput\n                inputMode=\"text\"\n                caption=\"Текстовая клавиатура\"\n                captionAlign=\"center\"\n            />\n        </div>\n    );\n}\n```\n\n## Работа с OTP-кодом из СМС Работа с OTP-кодом из СМС Функционал  Web OTP API  актуален только в мобильных браузерах.\nДля корректного автозаполнения кода СМС должна быть отправлена в следующем формате: ```\nВаш код подтверждения: 123456.\n\n@plasma.sberdevices.ru #123456\n```\n\nПервая и вторая (пустая) строки являются необязательными и предназначены для удобства чтения пользователем.\nПоследняя строка обязательна и должна состоять из: доменной части URL-адреса веб-сайта, который вызвал API; обязательно начинается с символа  `@` затем следует OTP, перед которым стоит знак  `#` ## Таблица свойств Таблица свойств"
}
