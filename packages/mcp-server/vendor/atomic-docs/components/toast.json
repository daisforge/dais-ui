{
  "name": "Toast",
  "slug": "components/toast",
  "category": "components",
  "url": "https://plasma.sberdevices.ru/sdds-finai/components/toast/",
  "content": "# Toast Toast :::caution Устаревшие значения свойства  `view` \nЗначения  `dark`  и  `light`  являются устаревшими. \nДля изменения фона  `Toast`  используйте компонент  `ViewContainer`  (см.  пример использования )\n::: ## Система подсказок Система подсказок Существует готовая система текстовых подсказок, для использования которой необходимо обернуть приложение в  `ToastProvider` : ```jsx\nimport React from 'react';\nimport { ToastProvider } from '@salutejs/sdds-finai';\n\nexport const App = () => (\n    <ToastProvider>\n        <div className=\"App\">Hello, world!</div>\n    </ToastProvider>\n);\n```\n\nПосле этого, с помощью хука  `useToast`  можно получить доступ к управлению подсказками. ```jsx\nimport React from 'react'\nimport { useToast, Button } from '@salutejs/sdds-finai'\n\nexport const MyComponent = () => {\n    const { showToast, hideToast } = useToast()\n\n    const onHide = () => {}\n\n    return (\n        <Button onClick={() => showToast({ text, position, timeout, fade, contentLeft, role, onHide })} />\n    )\n}\n```\n\n`showToast`  имеет необязательный аргумент  `position` , отвечающий за позиционирование подсказки. ```ts\ntype Position = 'top' | 'bottom';\n```\n\n`showToast`  имеет необязательный аргумент  `timeout` , отвечающий за время отображения подсказки.\nЕсли аргумент не передается, по умолчанию подсказка отображается пока пользователь ее не закроет. Скрыть текущую подсказку в любой момент можно с помощью функции  `hideToast` . ## Аргументы функции `showToast` Аргументы функции  `showToast` | Название | Тип             | Значение | Описание           |\n|----------|-----------------|----------|--------------------|\n|  `args`    |  `ShowToastArgs`  |          | Конфигурация toast | ```ts\nimport type { ReactNode } from 'react';\n\nexport interface ShowToastArgs {\n    /**\n     * Текстовая надпись\n     */\n    text?: string;\n    /**\n     * Слот для контента слева, например `Icon`\n     */\n    contentLeft?: ReactNode;\n    /**\n     * Место отображения подсказки\n     */\n    position?: ToastPosition;\n    /**\n     * WAI-ARIA роль\n     */\n    role?: ToastRole;\n    /**\n     * Отображать ли подложку блока подсказки\n     */\n    fade?: boolean;\n    /**\n     * Значение для дополнительно отступа блока подсказки, считается в rem.\n     */\n    offset?: number;\n    /**\n     * Блок подсказки c округлым border-radius\n     */\n    pilled?: boolean;\n    /**\n     * Отображать ли иконку закрытия\n     */\n    hasClose?: boolean;\n    /**\n     * Размер блока подсказки\n     */\n    size?: string;\n    /**\n     * Вид блока подсказки\n     */\n    view?: string;\n    /**\n     * Время отображения подсказки. Если не передать значение, подсказка будет отображаться пока ее не закроют.\n     */\n    timeout?: number;\n    /**\n     * Цвет текста (по умолчанию берётся цвет из view)\n     */\n    textColor?: string;\n    /**\n     * callback срабатывающий в момент события hideToast\n     */\n    onHide?: () => void;\n    /**\n     * callback срабатывающий в момент события showToast\n     */\n    onShow?: () => void;\n}\n```\n\n## Пример использования Пример использования Для изменения цветовой темы компонента  `Toast`  используйте свойство  `view`  компонента  `ViewContainer` . ```jsx\nimport React from 'react'\nimport { useToast, ToastProvider, Button, ViewContainer } from '@salutejs/sdds-finai'\n\nexport function App() {\n\n    const ToastExample = () => {\n        const { showToast } = useToast()\n\n        const onShow = () => {}\n        const onHide = () => {}\n        const toastData = {\n            text: 'Подсказка вызванная showToast',\n            textColor: 'cyan',\n            position: 'bottom',\n            role: 'status',\n            hasClose: true,\n            fade: false,\n            size: 'm',\n            view: 'default',\n            timeout: 3000,\n            onShow,\n            onHide\n        }\n\n        return <Button onClick={() => showToast(toastData)} text=\"Вызвать подсказку\" />\n    }\n\n    return (\n        <div style={{ display: 'flex', gap: '1rem' }}>\n            <ViewContainer view=\"onLight\">\n                <ToastProvider>\n                    <ToastExample />\n                </ToastProvider>\n            </ViewContainer>\n\n            <ViewContainer view=\"onDark\">\n                <ToastProvider>\n                    <ToastExample />\n                </ToastProvider>\n            </ViewContainer>\n        </div>\n    )\n}\n```\n\n## Вид иконки Вид иконки Значения  `positive`  и  `negative`  свойства  `view`  изменяют цвет иконки. \nЧтобы цвет применился, установите в иконке свойство  `color`  со значением  `inherit` : ```jsx\nimport React from 'react'\nimport { IconBell } from '@salutejs/plasma-icons';\nimport { useToast, ToastProvider, Button } from '@salutejs/sdds-finai'\n\nexport function App() {\n    const Icon = () => <IconBell color=\"inherit\" size=\"xs\" />;\n\n    const ToastExample = () => {\n        const { showToast } = useToast()\n\n        const onShow = () => {}\n        const onHide = () => {}\n        const toastData = {\n            contentLeft: <Icon />,\n            text: 'Подсказка c иконкой',\n            position: 'bottom',\n            role: 'status',\n            hasClose: true,\n            fade: false,\n            size: 'm',\n            view: 'positive',\n            timeout: 3000,\n            onShow,\n            onHide\n        }\n\n        return <Button onClick={() => showToast(toastData)} text=\"Вызвать подсказку\" />\n    }\n\n    return (\n        <ToastProvider>\n            <ToastExample />\n        </ToastProvider>\n    )\n}\n```"
}
